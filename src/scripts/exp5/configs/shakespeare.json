{
    "description": "Shakespeare character-level language model configuration for exp5",
    "note": "All trackers log EVERY step (every: 1) for complete trajectory analysis",
    "hyperparameters": {
        "batch_size": 64,
        "replacement": true,
        "block_size": 128,
        "n_embd": 64,
        "n_head": 4,
        "n_layer": 2,
        "dropout": 0.1,
        "note": "NanoGPT with ~100K parameters"
    },
    "tracking": {
        "note": "All trackers use every: 1 to log every step",
        "stage1_sgd": {
            "metrics": {"every": 1},
            "weights": {"every": 1}
        },
        "stage2_gd": {
            "metrics": {"every": 1},
            "weights": {"every": 1}
        },
        "stage3_sgd": {
            "metrics": {"every": 1},
            "weights": {"every": 1},
            "hessian": {"every": 1}
        }
    }
}
