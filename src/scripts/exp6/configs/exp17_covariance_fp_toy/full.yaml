experiment_id: EXP17
result_name: exp17_covariance_fp_toy_full
name: Covariance FP toy
description: Ensemble covariance evolution test showing that the discrete/raw-moment FP recursion matches exact SGD better than the standard FP truncation.
seed: 42
deterministic: true
device: cpu
results_root: src/scripts/exp6/results
parameters:
  dim: 10
  num_steps: 1000
  eta: 0.15
  sigma_g: 0.5
  eig_min: 0.3
  eig_max: 3.0
  n_runs: 50000
  initial_scale: 1.0
  eval_every: 10
