experiment_id: EXP17
result_name: exp17_covariance_fp_toy_smoke
name: Covariance FP toy smoke
description: Smoke test comparing empirical SGD covariance evolution with standard FP and discrete/raw-moment FP recursions.
seed: 42
deterministic: true
device: cpu
results_root: src/scripts/exp6/results
parameters:
  dim: 6
  num_steps: 100
  eta: 0.15
  sigma_g: 0.5
  eig_min: 0.3
  eig_max: 3.0
  n_runs: 10000
  initial_scale: 1.0
  eval_every: 2
