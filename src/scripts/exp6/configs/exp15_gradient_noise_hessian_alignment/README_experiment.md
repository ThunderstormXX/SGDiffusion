# EXP15 Gradient-Noise / Hessian Alignment

Question: does the empirical minibatch gradient-noise variance in the mean
Hessian eigenbasis scale with positive Hessian eigenvalues?

This experiment is artifact-based by default: it reads saved EXP2 `raw_outputs`
and produces an independent diagnostic CSV/figure/metrics package.

