experiment_id: EXP18
result_name: exp18_learning_rate_scaling_toy_smoke
name: EXP18 learning-rate scaling toy smoke
description: Smoke test for eta scaling of centered covariance and raw second moment.
seed: 42
deterministic: true
device: cpu
results_root: src/scripts/exp6/results
parameters:
  mode: toy
  dim: 6
  n_runs: 5000
  etas: [0.01, 0.03, 0.1, 0.2]
  sigma_g: 0.5
  sigma_h: 0.0
  eig_min: 0.3
  eig_max: 3.0

