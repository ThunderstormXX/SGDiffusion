experiment_id: EXP10
result_name: exp10_noise_alpha_sweep_full
name: Raw SGD-noise alpha sweep
description: Sweep alpha in g_full + alpha * (g_batch - g_full); alpha=1 recovers ordinary SGD.
seed: 52
deterministic: true
device: cpu
results_root: src/scripts/exp6/results
dataset:
  name: mnist
  data_dir: ./data
  sample_size: 1024
  val_size: 1000
  subset: first
  normalize: false
training:
  batch_size: 32
  replacement: true
  lr: 0.15
  reference_steps: 1024
ensemble:
  steps: 2048
  lr: 0.08
analysis:
  eval_every: 256
model:
  type: cnn
  input_downsample: 14
  conv_channels: [8, 16]
  conv_use_bn: false
  pool_after: [true, true]
  gap_size: 1
  mlp_hidden_dim: 32
  mlp_num_layers: 1
sweep:
  alphas: [0.0, 0.1, 0.5, 1.0]
  seeds: [52, 152]
