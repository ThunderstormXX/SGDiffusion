experiment_id: EXP12
result_name: exp12_langevin_verification_smoke
name: Langevin implementation verification smoke
description: "Diagnostic audit for standard and modified Langevin baselines: noise scaling, one-step matching, horizon errors, and modified-noise calibration."
seed: 62
deterministic: true
device: cpu
results_root: src/scripts/exp6/results
dataset:
  name: mnist
  data_dir: ./data
  sample_size: 256
  val_size: 256
  subset: first
  normalize: false
training:
  batch_size: 32
  replacement: true
  lr: 0.05
  reference_steps: 8
ensemble:
  n_runs: 4
  steps: 40
  lr: 0.09
analysis:
  one_step_samples: 8
  langevin_noise_batches: 3
  horizons: [1, 5, 20]
  n_projection_directions: 4
  calibration_multipliers: [0.5, 1.0, 1.5]
