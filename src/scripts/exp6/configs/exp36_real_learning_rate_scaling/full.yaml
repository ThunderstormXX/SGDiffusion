experiment_id: EXP36
result_name: exp36_real_learning_rate_scaling_full
name: EXP36 real learning-rate scaling full
description: Real MLP386 eta-scaling of raw and centered minibatch-gradient update moments.
seed: 42
deterministic: true
device: cpu
results_root: src/scripts/exp6/results
dataset: {name: mnist, sample_size: 1024, val_size: 256, subset: first, normalize: false}
training: {batch_size: 64, replacement: true, lr: 0.1, reference_steps: 30}
parameters:
  mode: mlp386
  batch_size: 64
  n_batches: 64
  n_runs: 64
  etas: [0.01, 0.03, 0.1, 0.2]

